<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ShippingRelease.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ShippingRelease.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="ShippingRelease">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />
    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate local:RegisterPage}"
        Route="RegisterPage" />
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:HomePage}"
        Route="HomePage" />
    <ShellContent
        Title="Shipping"
        ContentTemplate="{DataTemplate local:ShippingReleasePage}"
        Route="ShippingReleasePage" />

</Shell>
